# Optional: override API base. If unset, frontend uses /api (Vite dev proxy in dev, or same-origin in prod).
# Production (e.g. EC2 behind nginx): set to your API path, e.g. /lumina/api or https://yourdomain.com/lumina/api
# VITE_API_BASE_URL=/lumina/api
# Local dev: default /api is proxied to localhost:8000 by vite.config.js; override only if backend runs elsewhere.
# VITE_API_BASE_URL=http://localhost:8000
# Base path for assets and app root. Use /lumina/ for alignmentatlas.online/lumina; use / for local dev at root.
VITE_BASE_PATH=/lumina/api
