# CI: run tests on PRs targeting main and on push to staging.
# On push to main, tests run inside Deploy Staging / Deploy Production instead.
name: CI

on:
  push:
    branches: [staging]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test
    uses: ./.github/workflows/run-tests.yml
